{
  "name": "up-smart-traffic-platform",
  "version": "1.0.0",
  "description": "Root package.json for Render deployment",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
